# Perfil de worker falso — usado pelos testes do harness e por qualquer
# verificação do protocolo que não deva custar dinheiro nem depender de rede.
# O argv é relativo ao catálogo do harness (não ao cwd do worker). Quando o
# launcher opera sobre um repositório alvo, esses paths são resolvidos contra
# a raiz do catálogo em runtime.
id: fake-worker-v1
agent: fake
argv:
  - node
  - fixtures/fake-worker.mjs
prompt_delivery: argv
kill_after_seconds: 2
forbidden_flags:
  - --resume
  - -r
  - --continue
  - -c
  - --fork-session
  - --session-id
env_allowlist:
  - PATH
  - HOME
  - LANG
  - LC_ALL
  - AGENTLAB_FAKE_MODE
env_extra: {}
control_markers: {}
notes:
  - 'Não fala formato de provider nenhum: exercita a interface interna do harness.'
