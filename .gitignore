node_modules/
dist/
coverage/

# Runtime do harness de sessões descartáveis — NÃO versionado.
# A definição autoritativa das tarefas vive em dev/plan.yaml.
.dev/

# Inbox do worker (report + handoff draft). Separado do runtime do
# orquestrador de propósito: entrada do worker não é evidência.
.dev-inbox/

# Evidência de runs (gerada, reconstruível)
data/

*.log
.DS_Store
