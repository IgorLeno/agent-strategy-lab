/**
 * Configuração do repo-alvo: leitura e escrita de `.agentlab/project.yaml`, e
 * resolução do data dir (default `<lab>/data/`, sobrescrito por
 * `AGENTLAB_DATA_DIR` ou config).
 *
 * Fronteira: `agentlab init` cria SOMENTE `.agentlab/project.yaml` no repo do
 * usuário. Nada mais do projeto alheio é tocado.
 *
 * Preenchido por M33 (com o data dir já em M11).
 */
export {};
