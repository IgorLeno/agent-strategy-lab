/**
 * Evidência em disco: run directory, append JSONL, manifests por seção,
 * ledger, verificação de integridade, redaction e o índice SQLite derivado.
 *
 * Fronteira: o disco é a fonte de verdade; o índice SQLite é derivado e
 * descartável (reconstruível por M31). Nenhum dado pode existir só no índice.
 *
 * Preenchido por M11–M17, M30 e M31.
 */
export {};
